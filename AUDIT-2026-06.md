# System-Wide Audit — musedashboard (2026-06)

**Master record of the section-by-section code audit, every confirmed finding, what's been fixed, what's deferred, and the decisions made along the way.** Read this first when resuming the audit.

- **Started:** 2026-06-02 · **Last updated:** 2026-06-04
- **Production: v4.33 LIVE.** Worker deployed at Version `51b000b0-363c-411b-ab5e-b63deb9f5608`.
- **✅ AUDIT COMPLETE (§1–§15) and ✅ ALL HIGHs now FIXED & shipped.** The post-audit fixes landed across **v4.20–v4.33** (Phases 1–3 of `NEXT-SESSION-KICKOFF.md`). What's still open is the pay-path **P0** + the §13 full Worker auth — both bundled into the **Helcim migration (Phase 4)** — plus low-pri deferrals.
- **POST-AUDIT FIXES SHIPPED (v4.20→v4.33):**
  - **v4.20** §15 PWA-shell (SW cache-error guard + offline fallback + maskable icon + referrer meta + pinch-zoom) + §14 staff app (price-before-Complete, focus-preserve-on-rerender, logout push-unsub, esc logo).
  - **v4.21** §11 schedule (Clear→`__none__` sentinel, copy-week `_repeats`, repeat-toggle default) + `avgServiceTime` memo (new `store.rev`).
  - **v4.22** §12 client security — the **2 HIGH** (fd-user privilege-escalation gate + fd-user stored XSS) + requireAdminCode **fail-closed** + removed the dead `settings.js` Activity-Log dup + recovery dup-id (preserve original record id). *(Policy items: fallback-PIN/lockout declined; permission-toggle wiring deferred.)*
  - **v4.23** Phase 3a — catalog app-owned; removed Square catalog sync + Square Bookings (the deferred §10 `squarePushBooking`/`product_type` items are resolved by REMOVAL).
  - **v4.24–v4.27** Phase 3b — customer directory → DO `customer:<id>` entity + dedicated **Customers tab** (folds in the §10 note-orphan/merge concern; Square dual-write kept until Helcim).
  - **v4.28** Phase 3c — the §14 staff-app `updateAssignment` whole-entry clobber HIGH (now `queue.assignmentPatch` + per-assignment field-merge on `queue.upsert`) + status wins (revert preserves the status timer; reverts logged to audit).
  - **v4.29–v4.33** UX: per-service status dots/pills (queue/turns/floor) + floor-plan overhaul (all-services tiles, tech avatars, turn-count badges, smart tech-drag, per-station-type tech capacity).
- **§13 backend:** ⚠️ **`RESTORE_TOKEN` is now SET (owner) → the anonymous `/state/reset`+`/state/restore` hole is CLOSED.** The broader §13 unauthenticated-Worker cluster (open `/square` proxy, `/state`, `/photos`, `/sms`, legacy WS relay) remains RECORDED/deferred → **bundled with the Helcim migration (Phase 4 = full Worker auth).** The v4.24/v4.26/v4.28 Worker deploys added the customer + assignment-patch ops only (not auth).
- **STILL OPEN / deferred:** pay-path **P0** (Paid-status policy + safe reversal + the cancelled-processor-transaction + reopen-leaves-record cases — see `PRIORITIES.md`, → Phase 4a); §13 full Worker auth (→ Phase 4); Phase-2 permission-toggle wiring; low-pri §9 (party-drop / saveAppt-atomicity / stale-day race) + §11 (whole-object schedule/station concurrency) + assorted LOW/cosmetic items in the blocks below.

---

## How the audit is run (method)

Each section is audited with a **multi-agent workflow**: 5–7 independent reviewers each take a distinct lens (correctness/money, data-integrity, silent-failure, race-condition, stale/dead code, ergonomics, improvements) over a coherent code slice, producing structured findings with `file:line` evidence. Every raw finding then goes through an **adversarial verification** pass (a separate agent tries to *refute* it and adjusts severity). Only `confirmed` findings are reported; `false-positive`/`none` are dropped.

**Cadence:** collect findings per section → report → fix the HIGH / easy-high-ROI items as hotfixes (with owner approval) → continue. Verify each fix in the preview (or in isolation for pure logic) before committing.

**Operational quirk:** a few runs had a transient block of verification agents fail to return structured output (`§5`, `§8`). Fix: **resume the same workflow run** (`Workflow({scriptPath, resumeFromRunId})`) — cached reviews + successful verifications return instantly; only the failed ones re-run.

**Standing rules** (from owner): commit freely; **`git push` needs explicit owner OK each time**; **`wrangler deploy` is the owner's job** (walk them through it). Verify in preview; a disconnected preview tab's dispatch stays local, but the preview's WebSocket reaches the **prod** DO, so never dispatch test writes that would hit production.

---

## ✅ What's been FIXED and shipped (audit changelog v4.11 → v4.15)

| Ver | Fix | Section |
|---|---|---|
| **v4.11** | **Tip forced onto the card** — collection now computed against **bill + tip** (gift→cash→Zelle→card fill bill+tip; card takes remainder); recorded `tenders` still sum to the bill with `tip` separate, so reports/reconcile/historical records unchanged. | live-bug |
| **v4.12** | Numpad cents `00` overflow rejects instead of truncating ($123.45→$1234.50); `saveRecord` persists `totalCost` from `ticketTotal` (refunds keep negative); historical-edit discount capped + toast; Square catalog push surfaces failures + **stable create idempotency key** (was `Date.now()` → duplicate Square items); item/fee delete confirm; `turns_skips` default; `turns_off` cleared on rollover. | §3/§4/§5/§6 |
| **v4.12** | **Observability:** dead-lettered writes toast + sync pill shows red **"Failed N"** (not green "Synced"); global `error`/`unhandledrejection` handler → throttled toast; version badge re-checked on tab-focus; `saveTurnsHistory` wrapped in try/catch. | §1/§2/§6 |
| **v4.12** | **Cleanup/docs:** removed dead `_numpadCallback`, `saveTurnsAndSync`, `archiveTurnsForToday`, `muse_last_reset_date` write; fixed CLAUDE.md (`checkAppVersion` = manual reload, no sessionStorage guard; `js/config.js`→`js/app/config.js`; documented `muse_failed_ops`) + README/ROADMAP fictional `dedupByLabel` parse-time claim. | §1–§3 |
| **v4.13** | **`config.set` stale-write guard (THE root cause)** — per-key numeric `updatedAt` stamp + reject strictly-older on client (`applyChange`) AND DO (`applyMutation`); rejected writes not broadcast; snapshot carries `configMeta`. Closes the "last save wins" clobber implicated in §1/§2/§4/§5/§6 incl. the **roster-wipe HIGH**. Backward-compatible (unstamped/equal writes apply). **Client + Worker.** | §1/§2/§4/§5/§6 |
| **v4.14** | **§7 reports hotfixes:** historical retail-only **double-count** (synthetic assignment + items/fees both summed → ~2×); **partial Square refund** now records the amount actually refunded (was "nothing recorded" while card money went back); **pay-period anchor** stabilized + made **DST-safe** (was sliding daily / starting in the future; also fixed a pre-existing DST off-by-one for configured start dates). | §7 |
| **v4.18** | **§11 staff/check-in/floor-plan hotfix batch (client-only):** (1) **HIGH floor-plan stored XSS** — customer names (Square-autofilled) rendered raw into the station tile (`floorplan.js:124`) and the "Not seated" tray (`:189`); now escaped via shared `escHtml`, along with service/tech names (`:98`), station labels, the floor staff row, and the staff-list/schedule/lightbox names in `staff.js`. (2) **`submitCheckin` double-tap guard** (`_submitting` self-releasing lock) — no more duplicate party + duplicate Square upserts on a bounced tap. (3) **floor same-station collision** — `collectFloor` now routes a doubly-claimed seat's loser to the "Not seated" tray instead of dropping it from the floor entirely. (4) **`seatCustomer`/`assignTechToStation` paid-bail** — a drop no longer reverts a `paid`/edit that synced in mid-drag. (5) **`deleteStaff` orphan scrub** (schedule/`_repeats`/turns_order/inactive_staff/svc_time_reset) + a **history warning** steering the operator to Deactivate (preserves payroll attribution) instead of hard-delete. (6) `toggleService` dead-args cleanup. | §11 |
| **v4.17** | **§10 Square hotfix batch (client-only):** (1) **HIGH POS deep-link gift double-charge** — `sq-pos-btn` disable now includes a staged gift, and `proceedSquarePayment` bails when a gift is applied (was: full bill charged to the card AND gift drawn down on return). (2) **HIGH Terminal cash/Zelle silent failure** — a failed `recordCashPayment`/`recordExternalPayment` now stamps `squareUnrecorded` on the record (threaded through `saveRecord`) and shows "Paid ✓ — cash/Zelle not logged to Square (will show in Reconcile)" instead of a clean ✓. (3) **HIGH XSS** — new shared `escHtml`/`escAttrJs` in `utils.js`; applied to the customer autocomplete (`buildDropdown` text + onmousedown), the Customer Directory list, and the Square team-member `<option>` labels. (4) `squarePushBooking` + `reprintTerminalReceipt` **stable idempotency keys** (drop `Date.now()` → no duplicate booking / double receipt). (5) **catalog pull pagination** (cursor loop — was first-page-only) + **fee-misclassification** fix (word-boundary `\b(fee\|surcharge\|charge)\b` + gated on non-APPOINTMENTS_SERVICE so a real service/"Coffee"/"Toffee" isn't bucketed as a fee). (6) `proceedTerminalPayment` **re-entry guard** (`_charging`). (7) `squarePushItem` keyed by **stable id, not array index**. (8) observability: `squareUpsertCustomer` warns on non-ok PUT/POST; `squarePullServices`/`syncSquare` return booleans so a silent pull failure no longer shows green "synced ✓". | §10 |
| **v4.16** | **§9 calendar hotfix batch (client-only):** (1) **`saveAppt` double-tap guard** — `_apptSaving` re-entry flag + Save-button disable in try/finally (was: a second tap during the multi-second Google write minted a fresh groupId → duplicate party + duplicate Square upserts) — the §9 **HIGH**. (2) **`deleteAppt` group fan-out** — expands via `_eventGroupRefs` + `Promise.allSettled`, surfaces partial failures (was: cancelled only the clicked copy → party/multi-staff bookings half-survived and re-appeared on next sync). (3) **silent calendar-load failures surfaced** — `calLoadAndRender` keeps prior events on a per-cal fetch failure instead of blanking the column (double-book risk) and flags the pill `error`; `calSilentSync` reports `error` (not green `ok`) when any cal failed. (4) **XSS escaping** — event-detail modal title/cal-name/phone, Tasks panel title/notes/list, and Square-customer autocomplete now escaped (new module-level `_escHtml`/`_escAttrJs`). (5) **reminders no longer fire** for already-checked-in / no-show appts (`apptsForReminders` gained the no-show + queue-match guards `apptsForTurns` had). (6) **`ensureFreshToken` waiter leak** — timeout now splices its dead waiter so the `length===1` gate re-fires (was: one refresh timeout wedged all calendar writes for the session). (7) **`_phoneQueueMatch`** excludes finished (complete/paid/done) queue entries so an earlier shared-phone walk-in can't stamp status onto a later appt. | §9 |
| **v4.15** | **§8 cash refund → drawer Cash-Out** (optional "returned cash from the drawer" checkbox, shown only when a drawer is open, logs a Cash-Out so the close reconciles); **gift-card A1** ticketId carry-through (edit no longer breaks balance-restore on reopen); **gift-card A2** `giftcard.save` stale-write guard (client + DO) — the last unguarded money op. **Client + Worker.** | §8 |

---

## 🔑 Decisions made (with rationale)

1. **`config.set` guard done as its own staged change** (not lumped with quick wins) given it touches the core write path + DO. Built backward/forward compatible so it was safe in any client/Worker deploy order. ✅ shipped v4.13.
2. **Cash tips go STRAIGHT TO THE TECH** (owner confirmed) → the cash drawer's `tenders.cash` (bill-only) is already correct; **do NOT** change it to `cashReceived − change` (that would create a phantom *short*). §8 "B1" is a non-issue for this salon.
3. **Cash sales ALWAYS via the Pay screen** (owner confirmed) → §8 "B3" (quick Mark-Paid cash invisible to drawer) doesn't apply in practice.
4. **Cash refunds DO happen** (owner confirmed) → built the §8 "B2" Cash-Out checkbox. ✅ shipped v4.15.
5. **"Paid" status policy = P0** (owner-requested): decide whether staff may mark Paid manually, consolidate the two divergent "→ paid" paths into one (tenders + gift draw-down + audit), and define a **safe reversal/un-pay** flow. See PRIORITIES.md. Ties together §5/§7/§8 findings.
6. **Gift-card A3** (refund doesn't reverse the redemption) **deferred** — it's part of the P0 pay-path consolidation; best paired with the §10 Square pay-flow audit.
7. **Kept `dedupByLabel`** (it's covered by a test) and just corrected the false docs claim, rather than deleting tested code.
8. **`integer-cents`** money model **deferred** (existing P3) — float drift is absorbed by `toFixed`/`Math.round` at charge/display boundaries; revisit only if real drift appears (or do it natively in TurnDesk).
9. **Backend (`worker.js`) cleanups deferred to §13** (dead legacy WS relay, unbounded `deletion:` markers, probabilistic audit pruning) so client and backend changes don't interleave mid-audit.

---

## 🟠 OPEN findings (not yet fixed), prioritized

### 🔴 HIGH — §12 security (RECORDED, NOT SHIPPED — owner-deferred 2026-06-03)
- **§12 — front-desk user CRUD is ungated → privilege escalation** (`auth.js:240-296`; leaf `settings.js:301`): the "Front Desk Users" Settings leaf has no `adminOnly` flag and `showAddFdUser`/`saveFdUser`/`deleteFdUser` perform NO role check, so a **frontdesk-role user can mint a new role:admin user with a self-chosen PIN, log in as it, and gain full financial control** (refunds, delete-txn, historical edits — `canDo()` returns true for everything when role==='admin'). Fix: `adminOnly:true` on the leaf + `if (getActiveUser()?.role!=='admin') return` bail at the top of all four CRUD fns + block non-admins assigning admin/manager.
- **§12 — stored XSS via fd_user name** (`auth.js:173`, `205`, `211`, `26`): names are synced config rendered raw into the user list, PIN viewer, and logged-in display (and `photo` into a `src=` attr). A `<img onerror>` name executes on every device, in the front-desk origin (Square session + DO write path). Fix: `escHtml`/`escAttrJs` (already in utils.js). *(Reachable by anyone given the gating gap above.)*

### ⚖️ Security POLICY items — §12 (owner to decide; RECORDED, no action taken)
- **Hardcoded fallback Manager PIN `1234`** (`config.js:4`, `auth.js:84/90/94/129-133`) — permanent well-known admin, no way to disable; it's also the bootstrap admin so it can't be blindly removed. Owner decision: replace with a chosen value / required first-run setup.
- **No PIN attempt lockout/throttle** (`auth.js:61-116`) — 4-digit PIN is brute-forceable; trade-off: a lockout on the shared front-desk iPad could lock out staff.
- **`viewReports`/`manageStaff`/`manageServices` permission toggles unenforced** (the §2 item) — wire to real `canDo()` gates across the panels they name, or remove the toggles.

### MEDIUM (worth doing; several fold into the P0)
- **§12 — `requireAdminCode` FAIL-OPENS** (`auth.js:137`): if the admin-code modal element is absent it runs `onSuccess()` — destructive actions bypass the gate. Fix: fail **closed** (deny + toast when the modal is missing).
- **§12 — Activity Log stored XSS** (`settings.js:106-117` `loadAuditLog`/`auditRowHtml`): the delete-**reason** free-text + name/by rendered unescaped. NB `settings.js loadAuditLog` appears to be a **dead duplicate** of `audit.js renderActivityLog` (same target element) — verify which is live, escape the live one (and/or delete the dup).
- **§12 — recovery restore mints a NEW id** (`recovery.js:63-69`): restoring a rejected `record.save` gives it a fresh id, bypassing the "never revive a deleted transaction" guard → **duplicate financial records**. Fix: preserve the original record id on restore.
- **§12 — `saveFdUser`/`deleteFdUser` no in-flight guard + whole-object `fd_users` write** (`auth.js:268-296`): concurrent edits lose a user / resurrect a deleted one (per-key stale guard doesn't help within the object).
- **§2 — 3 permission toggles are inert:** `viewReports` / `manageStaff` / `manageServices` are editable in Settings but **never enforced** by any `canDo()`. Either wire real gates or remove the toggles.
- **§4 — index-based item/fee edit/delete:** `updateItemField`/`removeItem`/`updateFeeField`/`removeFee`/`squarePushItem` key off the render-time array index; after a remote reorder (Settings list doesn't re-render on inbound `config.set`) an edit/delete can hit the **wrong row**. Fix: key by the existing `id` (services already do).
- **§4 — Square pull misclassifies services→fees:** any catalog item whose name contains "fee/charge/surcharge" is routed to FEES regardless of `product_type` (silent). Classify on `product_type`/category.
- **§5 — party discount capped at the anchor member's base:** a large flat multi-guest discount silently truncates → **overcharge**, and the footer "Party Total" shows a different number than what's charged. Cap against the whole-party base.
- **§5 — edit-lock close-overwrite + no override:** `closeGroupAssignModal` auto-saves with no stale check (can overwrite a concurrent edit); operator can't override a stuck lock.
- **§5/§8 — gift draw-down skipped via modal status-cycle finalize; gift reversal bypassed on modal reopen** → **P0** pay-path consolidation.
- **§7/§8 — refunding a gift-card-paid ticket doesn't reverse the redemption** (income drops, card stays drawn down) → **P0**.
- **§8 residual — true same-instant concurrent redemption of the SAME card across two devices** is still last-writer-wins (A2 closed the offline-replay clobber, not this). Would need per-redemption-id merge (cards have no redemption ids today).
- **§9 — `apptsForTurns` drops the WHOLE party from the Turns upcoming strip when ANY one member checks in** (`calendar.js:165-167`): the group-level `qm` short-circuit `return`s for the whole booking, so un-arrived guests / their tech lose the expect-cue. The Today's panel does this correctly (per-row). Fix: per-person matching (regroup by `museName`, compute each person's own queue match). *Deferred from v4.16 batch — behavioral, needs care.*
- **§9 — `saveAppt` multi-event write is non-atomic with no rollback** (`calendar.js:1214-1247`): a mid-loop Google failure (transient 5xx / token lapse between inserts of a multi-guest/multi-cal save) leaves partial new + stale old events; the generic "Save failed" invites a re-save that duplicates. The v4.16 double-tap guard removes the *impatient-retry* amplifier but not a genuine mid-loop API failure. Fix: two-phase (insert-all-new → then delete-old) or roll back this attempt's inserts on throw; always `calLoadAndRender` in a finally. *Deferred — riskier write-path change; stage separately.*
- **§9 — stale-day race** (`calendar.js:412-414` / `422-444` / `601-620`): a slow `events.list` for the old day can land into the module-level `_calEvents` after the user navigated (or the 60s `calSilentSync` timer captured the old `_calDate`) → wrong-day appointments shown, and check-in/confirm/no-show act on them. Self-heals within ≤60s but the timer reliably reopens the window. Fix: monotonic load-generation token in both `calLoadAndRender` and `calSilentSync` (bail before committing if superseded). *Deferred — stage separately.*
- **§10 — `squarePushBooking` loose-substring service match** (`square-pos.js:598`): books one-of-N / "Mani"⊂"Manicure" while toasting success. The v4.17 stable-key fix stops *duplicate* bookings but NOT the wrong-service pick. Full fix: resolve from the structured `museLines` svcId list (and confirm/name the service in the toast); fall back to exact/word-boundary text match only for legacy events. *Deferred — needs the museLines read + a confirm step.*
- **§10 — same-name customer merge orphans phone-keyed notes** (`square-customers.js:370`, `cleanupMergeGroup`): the "Same name" group can hold differing phones; deleting the non-kept profiles leaves their phone-keyed notes orphaned while the confirm text promises notes are kept. Fix: re-key removed profiles' notes onto the kept profile's phone (reuse `rekeyNotesByPhone`'s collision-safe `place()`), or make the confirm text conditional. *Deferred — owner chose the Recommended batch which excluded this; revisit.*
- **§11 — schedule "Clear" silently reverts to the recurring `_repeats` value** (`staff.js:316`): clearing a single repeat-driven day deletes only the explicit entry, so `getScheduleStatus` falls back to the recurring default and the day reappears as worked — calendar/turns off-duty keep showing the tech. Fix needs a one-off-blank sentinel (`sched[date][staffId]='__none__'` → `getScheduleStatus` maps to null) + a picker note. *Deferred — schedule-semantics change; owner chose Recommended (excluded it).*
- **§11 — whole-object `schedule` + `station_layout` writes clobber concurrent edits** (`staff.js:306/374/392`, `floorplan.js:265/489`): the per-key v4.13 stale guard protects the whole-object key, not within-object granularity, so two devices editing different days/stations → last-writer-wins on the entire map. *Deferred — architectural; needs a per-cell merge op (`schedule.setCell` / per-station key). Low contention in single-front-desk use.*

- **§14 — Muse Staff app `updateAssignment` whole-entry clobber (HIGH, money-adjacent)** (`js/app/staff.js:269-278`): a tech's Start/Complete/Reopen re-dispatches the WHOLE cloned entry with a fresh `updatedAt`, so a front-desk fee/item/discount edit (or another assignment's change) that hasn't yet propagated to the tech device is silently overwritten → undercounted sale. Same class as §11 floorplan `seatCustomer`. **NOTE: `updateAssignment` already re-reads the freshest LOCAL entry at tap time, so the residual window is network propagation lag — the durable fix is a DO-side per-assignment field-merge for `queue.upsert` (backend/`wrangler deploy`, group with §13).** Cheap client-only partial mitigation: a per-assignment paid-bail (skip if the target assignment is already paid) closes the mixed-entry revert-paid sub-case only.
- **§14 — Complete/Start with no price typed records the service at $0** (`staff.js:287-293`): the dashboard blocks PAY at $0 (so not finalizable), but it shows a $0 complete + skews the tech's daily total. Fix (client-only): require a price before Complete (toast "enter a price first"), mirroring the dashboard's pay validation.
- **§14 — full list re-render wipes the price input mid-typing** (`staff.js:126-136`/`280`/`476`): `store.subscribe(render)` rebuilds `#staff-list.innerHTML` on every sync, so a sync arriving while a tech types a price destroys the focused input (caret/keyboard lost; the value survives in `_priceDraft`). Frequent on a busy floor. Fix (client-only): skip/defer the list rebuild while a price input is focused, or save/restore focus+caret.
- **§14 — logout doesn't unsubscribe push** (`staff.js:409-410`): a signed-out device keeps receiving the prior tech's assignment alerts. Fix (client-only): call the push `/unsubscribe` on `staffSwitch`.

### §15 — PWA shell (RECORDED; 0 HIGH; mostly robustness/PWA-hygiene — all client-only, no deploy)
- **MEDIUM — SW caches error responses** (`sw.js:79-86`): `networkFirst` does `cache.put` with no `res.ok`/type check, so a transient 404/500 (e.g. mid-deploy) gets cached and then served offline → a broken module offline until the next online fetch overwrites it. Self-healing online; bites only in the transient-error-then-offline window. Fix: gate `cache.put` on `res.ok && res.type==='basic' && !res.redirected`, and add an offline navigation fallback to `index.html`.
- **MEDIUM — offline = unstyled/unusable** (`index.html:21`, `sw.js`): the app depends on the **Tailwind CDN at runtime** (no build step) + Google Fonts, which are cross-origin and not (cacheably) precached → a truly offline cold start renders an unstyled, broken app. Architectural (tied to no-build-step); real fix = vendor/precache Tailwind, which implies a build step. Note/accept.
- **MEDIUM — no CSP, no SRI** (`index.html:21-23`): no Content-Security-Policy meta (defense-in-depth for the recorded stored-XSS) and no Subresource Integrity on the Tailwind/Fonts CDNs (a compromised/MITM CDN runs arbitrary JS in the app origin). Caveat: a **strict** CSP is incompatible with the app's pervasive inline `onclick` handlers + Tailwind's eval — so a meaningful CSP needs the handler refactor; SRI is partially blocked by the versionless Tailwind CDN URL. Realistic: revisit alongside the broader security work.
- **MEDIUM — pinch-zoom disabled** (`index.html:5`): `maximum-scale=1.0, user-scalable=no` blocks zoom (a11y issue for low-vision staff/customers). Trivial to relax — but it's a deliberate kiosk choice (prevents layout messing on the customer-facing screen); owner tradeoff.
- **LOW:** offline cache-miss → `respondWith(undefined)` → raw network error, no nav fallback (`sw.js:85`); install precache swallows 404s silently (a renamed module is never precached, no signal) (`sw.js:52`); no Referrer-Policy meta (full dashboard URL leaks to the CDNs); both manifests omit `id` (implicit install identity); SW registration has no `updatefound`/`controllerchange` handling (the manual ↻ badge is the intentional model). 
- **COSMETIC:** stale `v2.72` SW header comment vs `CACHE_NAME muse-v4.19` (`sw.js:1`); only the 512 icon is maskable (no maskable 192 → downscaled/letterboxed adaptive icon on Android); PWA icons not precached; cache-control no-store META is redundant with the network-first SW; no iOS startup image.

### LOW (robustness / data-integrity)
- §14 (client-only unless noted): Complete/Start toast "Sent ✓" even when offline (the outbox replays on reconnect, so not lost — just optimistic wording); refunded sales still count toward the tech's daily total/history (`49-66`); logo URL interpolated unescaped into the staff-PDF `<img src>` (`243/252` — esc it); `registerPush` swallows all errors so the "turn on alerts" banner vanishes while alerts silently don't work (`436-449`); blocked-popup PDF export is a silent no-op; a tech whose PIN is a strict prefix of a longer PIN can't auto-submit (must press Enter — minor). COSMETIC: tech-typed price reflected unescaped into the input `value` (self-XSS only); double-tap Start/Complete is idempotent (no corruption); "Connecting…" login flicker; calculator seed drops an in-progress expression.
- §12 (all RECORDED, owner-deferred): local `_esc` helpers in audit/recovery/chat/appearance **don't escape quotes** (attribute breakout) and have drifted → replace with shared `escHtml`/`escAttrJs`. · audit filter chip **reflected XSS** (`audit.js:58`). · **photo/logo/fduser uploads have no size/type cap** (only staff photos cap 2MB) → big iPad camera photo can OOM the tab / bloat synced config (`photos.js:67-79/156-162`). · `saveFdUser` allows a PIN equal to the `1234` fallback (shadows Manager) (`auth.js:276-282`). · chat send no re-entry guard + whole-array last-write-wins (`chat.js:48-57`). · photo "saved ✓" toasts before the config write confirms; R2 photo/logo DELETE failures swallowed → orphaned blobs (`photos.js:216-245`). · recovery clears the dead-letter entry **before** confirming re-dispatch (`recovery.js:57-69`). · PIN auto-login `setTimeout` can double-fire on rapid input (`auth.js:82-116`); no wrong-PIN feedback until 6 digits. · **dead code**: `appearance.js` per-user theme editor (`63-121`, no UI entry); `settings.js loadAuditLog`+helpers (dup of `audit.js renderActivityLog`). · role-label drift (Manager fallback = 'admin' in checkPin vs 'manager' in the PIN viewer). · `deleteFdUser` native `confirm()`; SMS test panel no phone-number validation.
- §11 (deferred from the v4.18 batch): **`avgServiceTime` full-scans all records per assignment per render** (`servicetime.js:39`) — O(records×seats) on the 10s floor re-render; memoize per render pass. · `copyLastWeekSchedule` ignores `_repeats` → false "nothing to copy" for a repeat-driven week (`staff.js:391`). · `saveStaff` edit silently no-ops if the id was deleted on another device mid-edit (`staff.js:168`). · repeat-toggle resets to OFF every picker open → easy to set a recurring entry non-repeating by accident (`staff.js:281`). · re-entering check-in wipes a half-typed form; 5s confirm auto-return; same-contact toggle carries only the first name (drops typed phone/last). · **staff PIN stored plaintext in synced config + prefilled into a visible input** (`staff.js:118/170`) — collision only soft-warned. · native `confirm()` (deleteStaff/copyLastWeek); narrow `_repeats` DST edge. · `isServiceVisibleOnCheckin` dup (already §4-logged); `groupColorIndex` never resets (by-design tint).
- §10 (deferred from the v4.17 batch as low-ROI/edge or risky-for-live-data): **`isService = !product_type` default** (`square-catalog.js:142`) — a Square ITEM with no `product_type` imports as a SERVICE; flipping the default to `=== 'APPOINTMENTS_SERVICE'` was **deliberately NOT done** (this salon's services may not be typed APPOINTMENTS_SERVICE, so the flip could mis-bucket existing services into items on the next pull — needs owner confirmation of their catalog typing first). · catalog dup-guard collapses two distinct same-named Square items (adopt-only label match). · `syncSquareAppointments` `services[0]` fallback for an unmatched booking + `limit=100` no pagination + never refreshes an already-imported appt (reschedule/service change ignored). · `squareUpsertCustomer` cache `display` drift (uses `entry.name` vs structured names). · team-member loads cap at `limit=200` (single-salon unreachable). All recoverable; no money impact.
- §9: check-in dedup falls back to **name equality** when `calEventId` is absent (`calendar.js:856`/`889`) → two distinct guests with the *identical full name* collide (second silently skipped) — make it phone-aware before falling back to name. · group expansion (`_eventGroupRefs`/`_gatherParty`/`saveAppt` oldGroupRefs) only enumerates the **viewed day's** `_calEvents`, so a copy dragged to another day *in Google Calendar* (out-of-app) is missed by confirm/no-show/delete (`801-811`/`874-891`/`1207-1212`). · reminder **banners stack unbounded** and never auto-expire after the appt starts (`appt-reminders.js:58-69`) — cap visible + auto-dismiss past-start. · Tasks `loadTasksForList` `maxResults:100` is **not day-scoped** (`1274`) → a long running task list silently truncates; paginate or window `showCompleted`. · `showAddTaskModal` uses native `prompt()` (`1294`) — replace with an in-app modal (notes/due in one step; avoids iOS-PWA prompt quirks). · calendar-selector **outside-click silently discards** in-progress show/hide/reorder draft (`664`) — toast on discard or keep the popover open. · `calToggleConfirmed`/`calMarkNoShow` could use `Promise.allSettled` for clearer partial-failure messaging (`821`/`839`) — no consumer misbehaves today (all reads use `.some(...)`), so message-only.
- §1: `giftcard.save` was the last unguarded money op — **now fixed (v4.15)**. Remaining: **unbounded outbox** (no size cap → quota blowout can drop pending writes); HTTP-fallback rejection not dead-lettered; no client **seq gap-detection**; no cache schema-version gate; a **stale** rejection still acks silently with no toast (Batch B's toast is for hard errors only).
- §2: `boot()` has no error boundary (critical wiring runs last); offline cold-start defers rollover; setup wizard can pop spuriously on a fresh device; PIN buffer not cleared on Esc; calendar omitted from live re-render.
- §4: `deleteService` orphans references (hidden_*, staff.services, queue refs); `Date.now()` id collisions; negative prices accepted; no item/fee duplicate/required-field validation.
- §5: `queue.remove` not stale-guarded; deletion tombstone blocks a walk-in re-add; Square-return mark-paid keyed only on local status (possible double-record); lock heartbeat margins.
- §6: live vs next-day history **turn-count divergence** (skips not persisted into the archive); `turns_order` not de-duped; grid `billed` `toFixed(0)` vs export `toFixed(2)` mismatch; history-view fallback ordering; no recovery affordance for a lost roster; rollover half-complete residual (marker ordering).
- §7: partial-refund commission docking scales only service cost; gift-card redemption bucketed by pay-date vs bill date (daily headline drift, self-corrects over windows); refund cap is bill-only so a **card tip is never refundable** (no UI hint); refund can show a negative Check on a payslip; historical edit keeps stale tenders/squarePaymentIds; reports filter on `checkinTime` while reconciles use `completedAt||checkinTime`.
- §8: two devices can open two drawers (first count lost); concurrent cash-in/out can drop a movement; close-vs-movement race; `cash_drawer_history` full-array write can drop a shift; no close confirmation; duplicated reconcile math; off-registry gift-card dedup at checkout; no serial-uniqueness check.

### ⚪ LOW — cosmetic / a11y / dead code / improvements
- Toast: no `aria-live`/`role=status` and overwrites in place (rapid toasts lost). `partyLetterMap` wraps at 26. `formatElapsed` can render "NaNh NaNm". Elapsed timer 10s lag, never paused. `byName` locale non-determinism.
- §4: dead `isServiceVisibleOnCheckin` export (checkin.js re-implements it); ROADMAP stale paths; full-list `innerHTML` re-render on catalog mutation.
- §5: dead "Edit Services" modal; over-exported `DEFAULT_CATEGORIES`/`STATIONS`; queue-row badge uses cached `totalCost`.
- §6: duplicated billed/turn-counting logic across 4 places (turns grid, history, export, reports payroll).
- §7: dead `_refCells`/`txnToday()`; triplicated payroll derivation; no shared money-derivation helper; CSV missing UTF-8 BOM + bare `\n`; report HTML interpolates names without escaping.
- §8: unused imports in giftcards.js; `confirm()` vs `showWarnModal` inconsistency.
- §9: dead `desc` param in `calEventClick` (`calendar.js:744`, never read — notes re-derived from the live event); stale doc comment on `apptsForReminders` return shape (`120-121` says `{id,name,startMs}`, actually also returns `svc`/`techName` consumed by the banner). The native `confirm()` cancel and the off-duty-peek-resets-on-nav findings were **verified as non-issues** (confirm() matches the app-wide pattern; peek reset is documented intentional behavior).
- **§9-found, deferred to §10 (Square):** `squarePushBooking` (`square-pos.js:598`) picks the service by loose `includes()` substring on the summary → silently books **one of N** services (and can mis-hit "Mani" in "Manicure") while toasting success; derive from structured `museLines` instead. · `square-customers.js:208` shares the same autocomplete onclick escaping gap fixed in calendar.js v4.16.

### 🚨 §13 — Worker / Durable Object backend (RECORDED, NOT SHIPPED — needs `wrangler deploy` + owner direction)
**Root cause: the Worker has NO app-level authentication.** The Worker URL ships in the public GitHub-Pages PWA; the Origin gate is OFF by default and even ON passes any no-Origin request (curl), so it is not auth. 11 HIGH (44 confirmed total).

**HIGH — unauthenticated endpoints (anyone with the URL):**
- **`/square/*` open proxy** (`273-306`) — re-signs ARBITRARY Square API calls with the salon `SQUARE_TOKEN`: dump all customer PII, tamper catalog, read all payments/orders, and (if the token has PAYMENTS_WRITE — likely, since POS is used) **create refunds/payments / load gift-card value**. The single worst defect.
- **`/state/snapshot` open read** (`180-191`,`357-362`) — anonymous read of the ENTIRE dataset (customers, financial records, gift cards).
- **`/state/mutate` open write** (`363-370`,`491-601`) — anonymous write of any mutation (corrupt/delete queue, records, config, gift cards).
- **`/state/reset` + `/state/restore` OPEN when `RESTORE_TOKEN` is unset** (`378-394`) — the `env.RESTORE_TOKEN && …` short-circuit fails OPEN, so factory-reset/restore is anonymous unless the secret is set. **⚠️ CONFIRMED UNSET in prod (owner, 2026-06-03) → these destructive endpoints are LIVE-OPEN right now.** Cheapest mitigation (no deploy): `wrangler secret put RESTORE_TOKEN`.
- **`/photos` PUT/DELETE open** (`136-164`) — upload/delete/**overwrite arbitrary R2 keys incl. `backups/…`** (same bucket) → poison/destroy disaster-recovery backups; also serves uploader-controlled Content-Type from the Worker origin (host arbitrary HTML/JS) and has no size cap (R2 abuse).
- **`/sms/send` open** (`235-252`) — send texts on the salon's gateway (spam, carrier cost, number-blacklist) — no auth, no rate limit.
- **Legacy WebSocket relay** (`470-476`) — broadcasts ANY unrecognized WS message verbatim to every connected dashboard: dead code AND an unauthenticated injection/fan-out vector. (Deferred §1 item — remove it.)

**MEDIUM:** `/ai/ask` open → burns the Gemini key (cost) (`207-225`); `?salon=` lets anyone create/read/write arbitrary DO instances (`124`); `restoreFromBackup` permanently loses the audit log + is non-atomic (`deleteAll()` then repopulate) + a mutation during the reset/restore R2-put window is silently wiped (`676-712`); `applyMutation` trusts payload shape → a missing id writes `queue:undefined`/`record:undefined` poison keys (`491-557`); unbounded `deletion:` markers shipped in every snapshot → monotonic bloat (`542-545`/`616-617`, deferred §1); `alarm()` `mut:` pruning starves if the snapshot/R2 step throws (`633-653`).

**LOW:** clock-skew tie can let a stale `record.save` win (`46-48`); `record.delete` permanently blocks a later legitimate same-id `record.save` (resurrection guard, `533`); stale-rejected write acks `{applied:true, stale:true}` so the client can't tell "dropped" from "saved" (`580-585`); R2 backups only contain the newest 500 audit events (`621-622`); probabilistic audit prune (`Math.random()<0.1`, `564`, deferred §1) can overshoot; `schemaVersion` emitted but never validated (no migration hook); swallowed push/socket errors.

**COSMETIC:** stale "remove after cutover" / "current client sends {type:queue|config}" comments are now false (`333-335`); idempotency dedup re-returns seq but never re-broadcasts (a peer that missed the change stays out of sync on replay, `496-499`). Verified CLEAN: `nextSeq` atomicity, socket-set vs broadcast, alarm coalescing, the config.set stale guard.

**Remediation shape — OWNER CHOSE direction (3): a proper full-auth solution later, NOT piecemeal patching; audit paused here.** The tiers below remain available if a quick stopgap is ever wanted before the full solution: (1) *Immediate, no deploy:* flip `ORIGIN_GATE_ENABLED=true` (live secret, instant, reversible) — partial (blocks other websites, not curl); set `RESTORE_TOKEN`. (2) *Worker-only deploy, no client change:* reset/restore fail-CLOSED, block `/photos` writes to `backups/` + size/content-type caps, remove legacy WS relay, payload-shape validation, restore audit + atomicity. (3) *The real fix (coordinated client + Worker deploy):* a shared bearer token the PWA sends + the Worker checks on all sensitive endpoints, ideally + a `/square` path allow-list + minimal Square token scopes. (Caveat: a secret shipped in the public PWA only raises the bar — pair with the Origin gate; true per-user auth is a TurnDesk-scale change.)

---

## 🧵 Cross-cutting themes (status)

1. **"Last save wins" on settings data** — ✅ **FIXED** (v4.13 `config.set` guard) + gift cards (v4.15). All money/state write paths (records, queue, config, giftcards) are now stale-write guarded on client + DO.
2. **Silent failures** — ⚠️ **partly fixed** (dead-letter toast, "Failed N" pill, global error handler; v4.16 calendar-load surfacing; v4.17 surfaced **Square cash/Zelle record failures** at pay time + `squareUnrecorded` flag, catalog-pull `synced ✓` only when both loaders succeed, customer-upsert non-ok warnings). Residual: stale-rejection feedback, unbounded outbox, HTTP-fallback dead-letter, seq gap-detection, `saveAppt` partial-write (deferred §9 MEDIUM), Reconcile UI labeling of `squareUnrecorded` rows.
3. **Divergent "→ paid" pay paths** — ⏳ **OPEN (P0)**. The quick "Mark Paid"/modal status-cycle skips gift draw-down + tenders + audit; reopen skips gift reversal; refund doesn't reverse gift. **§10 added these inputs:** the POS deep-link "paid" path records **no tenders/tip/squarePaymentIds** (card sale lands in the "Other" mix bucket; Square-device tip invisible; no receipt-reprint/refund); **`muse_term_pending` is written but never read** → a reload mid-Terminal-charge silently abandons an in-flight (possibly-completed) charge; **gift is stashed on the entry *before* the charge** → an abandoned-then-Mark-Paid applies a stale gift amount; **gift draw-down + tenders/tip land on different entries** when a non-first party member is tapped (reconciles today, but a footgun). One consolidated pay path fixes the cluster.
4. **Doc drift** — ✅ flagged items fixed (CLAUDE.md, README, ROADMAP).
5. **Integer-cents** — deferred (P3).

---

## 📋 Sections — status & remaining

| § | Section | Files | LOC | Status |
|---|---|---|---|---|
| 1 | Core sync/state | store.js, sync.js, +DO | ~350 | ✅ audited; config guard shipped |
| 2 | Bootstrap/session | main.js, config.js, session.js | ~500 | ✅ audited |
| 3 | Utils | utils.js | ~440 | ✅ audited |
| 4 | Catalog | catalog.js | ~226 | ✅ audited |
| 5 | Queue + status | queue.js, status.js | ~1290 | ✅ audited |
| 6 | Turns | turns.js | ~1008 | ✅ audited |
| 7 | Reports/payroll/refunds/reconcile | reports.js | ~2096 | ✅ audited |
| 8 | Gift cards + Cash drawer | giftcards.js, cashdrawer.js | ~622 | ✅ audited |
| 9 | Calendar + appt reminders | calendar.js, appt-reminders.js | ~1379 | ✅ audited; v4.16 batch staged (HIGH + 6 fixes); 3 MEDIUM deferred (party-drop, saveAppt atomicity, stale-day race) |
| 10 | Square integration | square-customers.js, square-pos.js, square-catalog.js | ~1541 | ✅ audited; v4.17 batch (3 HIGH + pagination/keys/classification/guards/observability); v4.11 tip math re-verified SOUND; deferred: squarePushBooking service-match, note re-key, product_type default. Pay-path P0 inputs captured. |
| 11 | Staff / check-in / floor plan / service time | floorplan.js, staff.js, checkin.js, servicetime.js | ~1269 | ✅ audited; v4.18 batch staged (HIGH floor XSS + checkin guard + collectFloor collision + paid-bail + deleteStaff scrub). Deferred: schedule-clear sentinel, whole-object concurrency, avgServiceTime perf, PIN-plaintext, schedule/checkin LOWs. |
| 12 | Remaining features | settings, auth, recovery, audit, appearance, photos, chat, sms | ~1608 | ✅ audited; **2 HIGH + security cluster RECORDED, NOT SHIPPED (owner-deferred)** — fd_user privilege-escalation, fd_user XSS, requireAdminCode fail-open, Activity-Log XSS, recovery dup-id, + 3 policy items (fallback PIN, lockout, permission enforcement). |
| 13 | Worker / Durable Object (backend) | cloudflare/worker.js | ~713 | ✅ audited; **🚨 11 HIGH — Worker effectively UNAUTHENTICATED (open Square proxy, state R/W, photos, sms; reset open if no RESTORE_TOKEN; legacy WS relay). RECORDED, NOT SHIPPED — needs wrangler deploy + owner direction.** Closes deferred §1 items (WS relay, deletion bloat, prob. prune). |
| 14 | Muse Staff tech app | js/app/staff.js + staff.html | ~599 | ✅ audited; 2 HIGH (one root: `updateAssignment` whole-entry clobber — durable fix = DO field-merge, backend tier) + client-only MEDIUMs ($0-complete guard, focus-on-rerender, logout push-unsub). RECORDED; awaiting fix-scope decision. |
| 15 | PWA shell | sw.js, manifest.json, manifest-staff.json, index.html shell | ~2795 | ✅ audited; **0 HIGH**; 6 MEDIUM (SW caches error responses; offline=unstyled via Tailwind CDN; no CSP/SRI; pinch-zoom disabled) + LOW/cosmetic PWA hygiene. All client-only. RECORDED. |

---

## Owner live-verify still pending (v4.15)
- Two-device catalog/roster edit survives a reload (config guard).
- Gift-card edit → reopen its ticket → balance restores (A1).
- Cash-refund "returned cash from the drawer" checkbox → drawer expected drops (§8 B2).
- (Standing real-money checks from prior cycles: refund-to-Square, gift-card Terminal sale, Reconcile.)
