# Cloudflare Worker local state / logs — never commit (may hold account ids + live request data)
cloudflare/.wrangler/
cloudflare/tail.out

# Local scratch / review artifacts
.claude/review-*.diff
.claude/*.out
